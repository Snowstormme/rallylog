name: Add tournament subscriptions

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
      - run: pip install 'psycopg[binary]>=3.2,<4'
      - name: Apply tournament subscription schema
        env:
          DATABASE_URL: ${{ secrets.DATABASE_OWNER_URL }}
        run: |
          python - <<'PY'
          import os, re
          import psycopg
          url = re.sub(r'^postgresql\+[^:]+://', 'postgresql://', os.environ['DATABASE_URL'])
          with psycopg.connect(url) as connection:
              connection.execute(open('scripts/add_tournament_subscriptions.sql').read())
          print('Tournament subscription schema is ready.')
          PY
