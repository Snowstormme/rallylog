# Set a long random value before deploying publicly.
SECRET_KEY=replace-with-a-long-random-secret
# Leave empty for local SQLite, or use a PostgreSQL URL for deployment.
# DATABASE_URL=postgresql://user:password@host:5432/database
